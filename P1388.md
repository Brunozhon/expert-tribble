# P1388

This is an **interval DP** problem.

To efficiently compute ranges for this problem, we can use a prefix sum array. Then, we can DP over every possible left and right ending up to the whole sequence, which will be the answer. For the DP, we chose a pivot point and use DP for the left half and sum the right half, and multiply those two values together.
